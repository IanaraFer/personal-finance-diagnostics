{
  "name": "finance-diagnostics-mobile",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare:web": "node scripts/copy-web.js",
    "build": "npm run prepare:web && npx cap sync",
    "android": "npx cap open android",
    "ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/browser": "^6.0.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "typescript": "^5.6.3"
  }
}